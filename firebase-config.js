/**
 * firebase-config.js — Firebase configuration
 *
 * Replace the placeholder below with your Firebase Web API Key:
 *   Firebase Console → Project Settings → General → Web API Key
 *
 * NOTE: Firebase Web API keys are designed to be used in client-side code.
 * Security is enforced by Firebase Security Rules, not by hiding this key.
 * However, it's good practice to keep this file out of version control
 * to avoid accidental exposure in public repositories.
 */
const FIREBASE_CONFIG = {
  apiKey: 'YOUR_FIREBASE_API_KEY'
};
